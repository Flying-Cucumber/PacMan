#ifndef __ENTITIES_H__
#define __ENTITIES_H__
#include "terrain.h"
#include <stdbool.h>

struct pacman {
    int x
    int y
    slab slapo
    bool is_alive
    bool is_supersaiyen
};

struct ghost {
    int x
    int y
    slab slapo
    bool is_alive
};

#endif
